# dsh-mobile-layout bundle patch: mounts the plugin row into the host
# composition. File access is derived from the current session's server-owned
# Workspace registration; no directory path configuration is required.
- insert:
    - id: dsh-mobile-layout
      name: dsh-mobile-layout
