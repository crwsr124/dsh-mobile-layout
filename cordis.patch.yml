# dsh-mobile-layout bundle patch: mounts the plugin row into the host
# composition. Ships with NO roots — the file browser is fail-closed by
# default; deployments open directories by overriding the row config in
# their own cordis.patch.yml (whole-config replacement — the plugin fills
# missing keys with defaults).
- insert:
    - id: dsh-mobile-layout
      name: dsh-mobile-layout
